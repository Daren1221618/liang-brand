// ============================================================
// 亮品牌 · 预置示例数据
// 后端 API 模式下，种子数据由服务端处理，前端不再需要
// ============================================================

export function seedDemoData(): boolean {
  // 后端已处理种子数据，前端不再需要
  return false;
}
